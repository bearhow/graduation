package util;

import android.content.Context;
import android.graphics.Bitmap;
import android.graphics.Canvas;
import android.util.AttributeSet;
import android.widget.ImageView;

public class RoundImageView  {
    private int mBorderThickness;
    private Context mContext;
    private int defaultColor;
    private int mBorderOutsideColor;
    private int mBorderInsideColor;
    private int defaultWidth;
    private int defaultHeight;

    public RoundImageView(Context var1) {
        throw new Error("Unresolved compilation problems: \n\tSyntax error, annotations are only available if source level is 1.5 or greater\n\tSyntax error, annotations are only available if source level is 1.5 or greater\n");
    }

    public RoundImageView(Context var1, AttributeSet var2) {
        throw new Error("Unresolved compilation problems: \n\tSyntax error, annotations are only available if source level is 1.5 or greater\n\tSyntax error, annotations are only available if source level is 1.5 or greater\n");
    }

    public RoundImageView(Context var1, AttributeSet var2, int var3) {
        throw new Error("Unresolved compilation problems: \n\tSyntax error, annotations are only available if source level is 1.5 or greater\n\tSyntax error, annotations are only available if source level is 1.5 or greater\n");
    }

    private void setCustomAttributes(AttributeSet var1) {
        throw new Error("Unresolved compilation problem: \n");
    }

    protected void onDraw(Canvas var1) {
        throw new Error("Unresolved compilation problem: \n\tSyntax error, annotations are only available if source level is 1.5 or greater\n");
    }

    public Bitmap getCroppedRoundBitmap(Bitmap var1, int var2) {
        throw new Error("Unresolved compilation problem: \n");
    }

    private void drawCircleBorder(Canvas var1, int var2, int var3) {
        throw new Error("Unresolved compilation problem: \n");
    }
}


